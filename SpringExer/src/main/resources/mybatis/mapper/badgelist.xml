<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.mapper.badgelist">
<!-- 뱃지 삽입 -->
	<insert id="sqlinsertBadgeList" parameterType="badgeListDTO">
		INSERT INTO BADGELIST
		(BGID, NICKNAME, BGTITLE, BGCONTENT, BGDATE)
		VALUES
		(#{bgId}, #{nickName}, #{bgTitle}, #{bgContent}, #{bghDate})
	</insert>
<!-- 뱃지 조회 -->

	
<!-- 뱃지 수정 -->

<!-- 뱃지 삭제 -->
</mapper>