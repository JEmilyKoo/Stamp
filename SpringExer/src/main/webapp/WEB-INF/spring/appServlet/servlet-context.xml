<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:websocket="http://www.springframework.org/schema/websocket"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<!-- <resources mapping="/resources/**" location="/resources/" /> -->
	<default-servlet-handler />
	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	<!-- 작동 안 되어서 일단 빼둠 <beans:bean id="chatServer" class="com.company.exer.web.WebSocketServer"/> 
		웹소켓 서버와 클라이언트에서 접속하는 path와 매핑:엔드포인트 설정 <websocket:handlers allowed-origins="*"> 
		<websocket:mapping handler="StampServer" path="/chat-ws.do"/> </websocket:handlers> -->


	<!-- 파일 -->
	<context:component-scan
		base-package="com.company.exer" />
	<context:property-placeholder
		location="classpath:mybatis/paging.properties" />
	<!-- 업로드 패스 설정 -->
	<beans:bean class="java.lang.String" id="uploadPath">
		<beans:constructor-arg
			value="D:\LHJ\Workspace\Java\.metadata\.plugins\org.eclipse.wst.server.core\tmp3\wtpwebapps\SpringExer\resources" />
	</beans:bean>

	<!-- 일반 파일 업로드 경로 -->
	<resources mapping="/imgUpload/**"
		location="/resources/imgUpload/" />

	<beans:bean
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver"
		id="multipartResolver">
		<beans:property name="maxUploadSize" value="10485760" />
	</beans:bean>


	<!-- 오류나서 주석처리 <interceptors> <interceptor> <mapping path="/freeboard/*"/> 
		<beans:bean class="com.company.exer.service.AuthLoginInterceptor" id="AuthLoginInterceptor"/> 
		</interceptor> </interceptors> -->
</beans:beans>
